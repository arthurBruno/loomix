import { chakra } from '@chakra-ui/react';

const VerticalSeparator = chakra('div', {
  base: {
    width: '1px',
    height: '20px',
    backgroundColor: '#777777',
  },
});

export default VerticalSeparator;
